# 团队 Git 开发规范

> 适用对象：小浩和小霍。

## 一、分支命名

| 前缀 | 用途 | 例子 |
|---|---|---|
| `feat/` | 新功能 | `feat/login` |
| `fix/` | 修 bug | `fix/empty-password` |
| `refactor/` | 重构 | `refactor/payment-api` |
| `docs/` | 文档 | `docs/api-usage` |

规则：小写英文、连字符分隔、见名知意。

## 二、日常开发流程

**1. 每天开工先同步 main**

```bash
git checkout main
git pull
```

**2. 从 main 开功能分支**

```bash
git checkout -b feat/功能名
```

**3. 在分支上写代码，小步提交**

一个逻辑改动对应一次 commit，commit 信息写清楚做了什么：

```bash
git add .
git commit -m "修复xxx"
```

**4. 做完推送到远程**

```bash
git push -u origin feat/功能名
```

**5. 发 PR**

推送后到 GitHub 仓库页面点「Compare & pull request」：
- 标题：一句话说清这个 PR 做了什么
- 描述：写清楚「改了什么、为什么改、怎么验证」

**6. 发微信通知小光合并**

PR 建好后，**发微信通知小光合并**，由小光审批合并。自己不要点合并按钮。

**7. 合并完成后清理本地分支**

小光合并后，回到本地删除自己用过的分支：

```bash
git checkout main
git pull
git branch -d feat/功能名
git fetch --prune
```

## 三、PR 规范

- **一个 PR 只做一件事**：一个功能或一个 bug 对应一个 PR，不要混在一起。
- **控制规模**：一个 PR 大概是**一天内的工作量**，最多几百行改动。太大就拆成几个 PR 分批发。
- **分支别超过 3 天没合并**：做完了就立刻发 PR、通知合并，不要攒。

## 四、注意事项

- **不要直接 push 到 main**：main 已开启保护，推不上去；所有改动统一走 PR。
- **不要直接改 main**：开发永远在自己的功能分支上，`main` 只通过 PR 更新。
